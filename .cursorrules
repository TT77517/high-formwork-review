本项目使用多 AI Agent 协作开发。

## ⚠️ 开始工作前必须读（不读 = 重复造轮子）
1. AGENTS.md（协作约定与入口协议）
2. PROGRESS.md（当前进度与交接日志——看上一个 agent 做到哪了）
3. CODEX_CONTEXT.md（技术架构与禁止事项）
4. high-formwork-review/README.md（详细文档）

## 协议
- 签到：make checkin AGENT=cursor TASK="任务"
- 签退：make checkout AGENT=cursor DONE="完成" NEXT="下一步"
- 测试：make test
- 安装 hooks：make hooks

## Enforcement
- pre-commit hook：改代码不更新 PROGRESS.md → 拒绝提交
- commit-msg hook：message 不规范 → 拒绝提交
- 详见 AGENTS.md
